name: APP.TEST
on: workflow_dispatch

permissions:
  id-token: write
  contents: read

jobs:
  stealth-rdp:
    runs-on: windows-latest
    steps:
      - name: Tailscale Connect
        uses: tailscale/github-action@v3
        with:
          authkey: ${{ secrets.TAILSCALE_AUTHKEY }}
          tags: tag:ci 
          
      - name: Configure Ghost RDP
        run: |
          # 1. Enable RDP
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          
          # 2. Create the Hidden User (Name: AdminSystem / Pass: GhostPass123!)
          net user "AdminSystem" "GhostPass123!" /add
          net localgroup "Remote Desktop Users" "AdminSystem" /add
          net localgroup "Administrators" "AdminSystem" /add

      - name: Keep Alive
        run: |
          echo "TARGET_IP: $(tailscale ip -4)"
          echo "USER: AdminSystem"
          echo "PASS: GhostPass123!"
          Start-Sleep -Seconds 21600
